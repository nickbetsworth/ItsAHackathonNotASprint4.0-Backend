{
  "type": "module",
  "name": "whats-left-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node --no-warnings src/index.js",
    "dev": "nodemon --watch . --exec 'node --no-warnings src/index.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "fastify": "^5.3.2",
    "fastify-cli": "^7.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
